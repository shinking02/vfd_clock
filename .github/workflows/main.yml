name: My CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Install Python packages
      run: |
        pip3 install cpplint

    - name: Run cpplint
      run: |
        ./cpplint.sh
